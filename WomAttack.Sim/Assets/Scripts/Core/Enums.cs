public enum NodeType
{
    Internet,
    Router,
    Switch,
    PC
}

public enum NodeColor
{
    Green,
    Yellow,
    Red,
    Black
}

public enum SimulationPhase
{
    Scan,
    Exploit,
    Propagate
}
